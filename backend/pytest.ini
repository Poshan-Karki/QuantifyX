[pytest]
# Tests live in tests/ but import the app modules (verdict, schema, main, ...)
# as top-level names. pythonpath puts the backend root on sys.path so those
# imports resolve without needing an installed package or sys.path hacks.
testpaths = tests
pythonpath = .
