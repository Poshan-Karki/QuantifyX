# Base URL of the QuantifyX backend, no trailing slash.
# Vite inlines this at BUILD time, so it must be set on whatever host runs
# `npm run build` -- changing it later requires a rebuild, not a restart.
# Local development default (used when unset): http://localhost:8000
VITE_API_URL=http://localhost:8000
